@{
    ViewData["Title"] = "Panel de Cliente";
    Layout = null;  // usa tu _Layout si ya lo tienes
}

<div class="container py-4">
    <h1 class="mb-2">Hola, @User.Identity?.Name 👋</h1>
    <p class="text-body-secondary">Bienvenido al panel de cliente.</p>

    <div class="card rounded-4 shadow-sm mt-3">
        <div class="card-body">
            <h5 class="card-title mb-3">Acciones rápidas</h5>
            <ul class="mb-0">
                <li>Ver mis órdenes (próximo)</li>
                <li>Actualizar mis datos (próximo)</li>
            </ul>
        </div>
    </div>
</div>
