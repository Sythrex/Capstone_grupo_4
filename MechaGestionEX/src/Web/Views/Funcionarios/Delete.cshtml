@model Infrastructure.Persistence.Models.funcionario
@{
    ViewData["Title"] = "Eliminar Funcionario";
}

<div class="card shadow mb-4">
    <div class="card-header py-3 bg-danger text-white">
        <h6 class="m-0 font-weight-bold">Eliminar Funcionario</h6>
    </div>
    <div class="card-body">
        <p>¿Seguro que deseas eliminar este registro? Esto eliminará la asignación al taller actual, pero no afectará otras asignaciones o datos relacionados.</p>

        <dl class="row">
            <dt class="col-sm-3 mb-2">ID</dt>
            <dd class="col-sm-9 mb-2">@Model.id</dd>
            <dt class="col-sm-3 mb-2">RUT</dt>
            <dd class="col-sm-9 mb-2">@Model.rut</dd>
            <dt class="col-sm-3 mb-2">Nombre</dt>
            <dd class="col-sm-9 mb-2">@Model.nombre</dd>
            <dt class="col-sm-3 mb-2">Especialidad</dt>
            <dd class="col-sm-9 mb-2">@Model.especialidad</dd>
            <dt class="col-sm-3 mb-2">Activo</dt>
            <dd class="col-sm-9 mb-2">@(Model.activo ? "Sí" : "No")</dd>
            <dt class="col-sm-3 mb-2">Tipo</dt>
            <dd class="col-sm-9 mb-2">@(Model.tipo?.nombre ?? "Sin Tipo")</dd>
        </dl>

        <form asp-action="Delete" asp-route-id="@Model.id" method="post">
            <div class="d-flex justify-content-between flex-wrap">
                <button class="btn btn-danger me-2 mb-2" type="submit">Eliminar</button>
                <a class="btn btn-secondary mb-2" asp-action="Index">Cancelar</a>
            </div>
        </form>
    </div>
</div>